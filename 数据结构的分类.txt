常见的数据结构包括：数组、链表、栈、队列、哈希表、树、堆、图。

它们按照逻辑结构可以分线性结构和非线性结构两大类。

线性数据结构：数组、链表、队列、栈、哈希表，元素之间是一对一的顺序关系。
非线性数据结构：树、堆、图和哈希表。

对于哈希表，其底层实现是数组，所以归属于线性数据结构。但是为了解决哈希冲突，引入了链式地址，每个桶指向的链表再超过
一定长度时就会转化为树，所以也属于非线性数据结构。

值得注意的是，所有的数据结构都是基于数组、链表或二者的组合实现的。